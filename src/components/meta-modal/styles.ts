// Class strings shared across the meta-modal's split-out pieces (the modal shell, the collapsible
// keybind group, the info panel), kept here so the long Tailwind strings live in one place.
export const settingClass =
	"group relative flex w-full items-center justify-between rounded-[10px] bg-white/5 p-[15px_20px] mb-[15px] hover:bg-white/15 focus-visible:bg-white/15";

export const headingClass =
	"mb-[1em] text-center text-[40px] [&:not(:first-of-type)]:mt-[1.5em] after:mt-[0.25em] after:block after:h-px after:w-full after:bg-white/80 after:content-['']";
